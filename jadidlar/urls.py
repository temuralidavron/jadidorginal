from django.urls import path
# from .views import JadidListCreateView #, JadidDetailView

urlpatterns = [
    # path('api/jadidlar/', JadidListCreateView.as_view(), name='jadidlar-list-create'),
    # path('api/jadidlar/<int:pk>/', JadidDetailView.as_view(), name='jadidlar-detail'),
]
