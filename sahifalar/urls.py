from django.urls import path
# from .views import SahifalarListCreateView#, SahifalarDetailView

urlpatterns = [
#     path('api/sahifalar/', SahifalarListCreateView.as_view(), name='sahifalar-list-create'),
    # path('api/sahifalar/<int:pk>/', SahifalarDetailView.as_view(), name='sahifalar-detail'),
]
